/*
    MULTIPLICAÇÃO SIMPLES

        Você receberá dois valores inteiros. Faça a leitura em seguida calcule o produto entre estes dois valores. Atribua esta operação à variável PROD, mostrando esta de acordo com a mensagem de saída esperada.

        - Entrada: Contém dois valores inteiros.
        -Saída: Exiba a variável PROD.

*/

let valor1 = parseInt(gets()); // entrada do inteiro valor1
let valor2 = parseInt(gets()); // entrada do inteiro valor2
let total = 0;
total = valor1 * valor2;

console.log ("PROD = " + total);